<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ai.inquery.server.domain.repository.mapper.TaskMapper">

    <select id="pageQuery" resultType="ai.inquery.server.domain.repository.entity.TaskDO">
        select id, gmt_create, gmt_modified,
               data_source_id, database_name, schema_name, table_name, deleted, user_id, task_type, task_status
        from task
        <where>
            <if test="dataSourceId != null ">
                and data_source_id = #{dataSourceId}
            </if>
            <if test="databaseName != null and databaseName != '' ">
                and database_name = #{databaseName}
            </if>
            <if test="tableName != null and tableName != '' ">
                and table_name = #{tableName}
            </if>
            <if test="schemaName != null and schemaName != '' ">
                and schema_name = #{schemaName}
            </if>
            <if test="userId != null ">
                and user_id = #{userId}
            </if>
            <if test="taskType != null and taskType != '' ">
                and task_type = #{taskType}
            </if>
            <if test="taskStatus != null and taskStatus != '' ">
                and task_status = #{taskStatus}
            </if>
            <if test="deleted != null ">
                and deleted = #{deleted}
            </if>
        </where>
    </select>
</mapper>
